# 节点人工验收闭环

沿用独立业务服务、能力网关及只读Agent设计。本阶段只提供验收通过，不实现退回返工或交期变更审批。

1. 测试角色、报完工前置状态、版本冲突、重复验收、说明长度及完工数量舍入边界。
2. 业务领域新增验收记录，复用事务幂等和Outbox；适配器注册固定 `sampling.node.accept` 能力。
3. 桌面仅向项目负责人展示验收按钮，确认弹窗展示节点、版本及效果；验收记录可追溯。
4. 数据库集成测试验证幂等与事件一次写入；真实演示报完工、验收、事件分析及页面更新，保持模型无写工具。
