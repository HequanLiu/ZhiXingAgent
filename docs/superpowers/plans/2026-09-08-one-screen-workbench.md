# 原型一屏工作台实施计划

依据用户提供的 docs/原型.png，直接调整现有前端，沿用打样领域和真实数据。

1. 左侧改为目标、当前概况、助理任务、最近同步、固定输入区。输入支持本地订单定位和功能入口，不冒充模型对话或触发外发。
2. 中间压缩标题和指标，列表每页4单，七节点详情同时展示。交期依据、验收记录和完整分析保留弹窗入口。
3. 右侧展示跨订单异常预览和所选订单最新反馈；长内容区域内滚动。桌面布局固定视口，移动反馈页面保持自然滚动。
4. 1366×768、1536×1024及390×844浏览器验收：无页面横向溢出，桌面无整页纵向滚动，输入可见且订单定位有效，原有弹窗入口可用。执行类型检查与构建。

## 验收结果

2026-09-08：上述两个桌面尺寸均无整页滚动，4行订单和7个流程节点可同时显示；节点区域无需滚动。输入 A26-021 可定位对应订单；交期/原始记录和分析弹窗通过。390px移动反馈页无横向溢出。TypeScript和Vite生产构建通过（原有主包体积警告仍在）。

验证脚本 `scripts/layout-smoke.mjs`；截图及尺寸/交互记录 `.runtime/layout/`。本轮仅改变前端布局与本地快捷指令，不请求模型、不修改业务事实、不发送渠道消息。此前发布压缩包未重新打包，本次变更以当前工作树和本地页面为准。
