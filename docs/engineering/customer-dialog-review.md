# 客户明细与弹框验收（2026-09-08）

覆盖功能菜单四个管理页面，以及全部 16 个 BusinessModal 入口。现有接口、身份权限、版本校验和业务动作保持。

## 改造结果

- 共用弹框标题、说明、内容滚动区、底部操作和紫色主题；窄屏表单单列，表格内部横向滚动。
- 异常支持搜索、状态筛选及历史跟进；巡检分为设置、站内提醒、经理升级和企微记录。
- 运行管理分为设置、场景连接、统计；高级参数折叠。人员列表姓名优先，编辑独立展示。
- 反馈记录采用倒序时间线，保留节点、人员、原始数量、阻塞和预计日期；验收返工及交期依据分别展示。
- 分析结论区分历史版本，业务快照展示查询时间和节点卡片；增加加载、失败重试与空状态，并隔离关闭后的晚到响应。
- 新建与流程长表单使用固定底部提交按钮；其余表单采用可见的吸底操作区。

## 验证

- `pnpm.cmd typecheck`：通过。
- `pnpm.cmd test`：91/91 通过，包含新增的 3 项证据展示测试；新测试已观察到 RED 后转 GREEN。
- `pnpm.cmd build`：通过。仍有单个前端包超过 500 kB 的构建提醒。
- `node scripts/healthcheck.mjs`：web、platform、business、worker 全部 ready。
- 浏览器：默认桌面 1280×720，以及手机 390×844。逐项检查管理分组、反馈/证据、项目、流程、附件、变更、验收、分配及二级跟进/人员编辑弹框。过程中截图直接在工具中查看。
- 真实页面：验证管理页数据与标签切换、异常无匹配提示、反馈及交期记录；未提交业务更改。
- 隔离页面 `tests/dialog-review.html`：使用内存数据验证百分比反馈载荷、流程配置外置按钮提交、项目空表单校验、异常说明必填、退回原因约束、分析证据成功/失败重试/空态及读取中关闭后不重开。
- 已修复手机巡检内容横向溢出；复核内容 scrollWidth 与 clientWidth 相等。数据表格保留自身水平滚动。
- 只读审查涵盖请求与权限保持、表单字段保留及异步竞态。修正“完成后仍需经理验收”的文案。

隔离页面可在仓库根目录通过 `pnpm.cmd exec vite --host 127.0.0.1 --port 5180 --strictPort` 启动，再访问 `/tests/dialog-review.html`。该页面不包含在生产入口中，模拟提交不会写入业务数据库。

本次未请求真实模型、未发送企业微信消息，也未重复运行会新增订单的业务 smoke 脚本。已启动的开发服务保持运行。
