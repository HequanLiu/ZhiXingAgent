# 声研实验室验收记录

## 最终本地交付验收（2026-09-07 18:04，优先于下方历史记录）

范围按用户最新选择为企业微信和本次自研打样模块。以下本地验证已执行；尚未进行正式服务器部署、真实企微回调或真实消息发送。

- **全量回归 123/123 通过，0 失败、0 跳过**。覆盖业务版本/权限/幂等、审批续行、模板批量原子导入、WeCom 加密协议及模拟传输、成员撤权、ALS 租户并发隔离、实际 Gateway/Registry 并发渠道回调、迁移回滚及历史校验。日志 `.runtime/verification/final-tests.log`。
- **TypeScript 与生产构建通过**。主包 1,043.61 kB，gzip 331.49 kB；保留大于500kB的构建体积警告，尚未进行专项性能优化。
- **真实两库迁移通过**：业务 `001-baseline.sql`；平台 `001` 至 `005-members.sql`，已有数据保留。目录具备动态角色/启停/版本、最后经理保护及不可修改审计；不按旧用户名自动推断经理。
- **运行健康通过**：2026-09-07T09:59:32Z 网页、平台、业务和 Worker 全部 ready。以进程覆盖启动 `SOUNDLAB_MODEL_ANALYSIS=false`、`WECOM_SEND_ENABLED=false`，本轮无新增模型调用和真实企微发送。
- **真实 HTTP 业务闭环通过**：订单 `E2E-1788775172674` 创建→配置→附件字节验证→百分比反馈→交期预测→人工批准→幂等执行→验收，最终 v7，记录 `.runtime/acceptance-smoke.json`。
- **真实桌面和移动浏览器操作通过**：1440px桌面模板保存/批量导入/连接管理入口，390px移动数量反馈；订单 `UI-1788775182262` 与 `-B`，两者均已验收归档。无捕获到的 pageerror。截图 `.runtime/browser/desktop.png`、`mobile.png` 已目视检查，记录 `.runtime/browser/acceptance.json`。
- **人员管理浏览器操作通过**：新增 `ui-member-1788775415853`，动态身份请求成功；页面停用后请求立即返回403。记录 `.runtime/browser/members.json`。另一脚本定位失败产生的测试成员也已停用；所有审计保留。
- **审批后自动续行实际验证**：任务 `04d46dbe-ac2d-46b6-93fe-debef8eae4c2` 经人工批准后由Worker执行并查询业务回执成功，记录 `.runtime/delegation-smoke.json`。动态身份改造后全量回归额外验证撤销经理权限会阻止待执行任务。此能力由平台持久任务实现，不等于 Harness SDK 原生审批回调。
- **双库真实备份与恢复通过**：`.runtime/backups/2026-09-07T10-04-05.749Z`，恢复校验业务40条订单、平台8条分析任务和8条成员记录（含停用测试成员），验证两库迁移 ledger。随机恢复库已删除，原库未恢复覆盖。测试订单保留完整记录，浏览器遗留四单已明确标注测试并验收归档。
- **独立评审已闭环**：动态demo名单拦截、旧目录bootstrap边界、回调持有主池时嵌套registry/身份读取等待均已修复。当前目录及registry使用独立有界池，8并发回调在主池max2的真实Gateway测试通过。

### 交付内容与外部依赖

模板库、批量导入、范围巡检/超时升级、企微入站/提醒队列、持久能力绑定、统一结果信封、动态人员角色、平台审批续行、版本化迁移、CI、健康检查和发布脚本已纳入本地交付。CI配置尚未推送执行；代码未提交或推送Git。

企业微信真实连接仍需 CorpID、AgentID、回调 Token/AESKey、应用 Secret、成员UserID和公网HTTPS回调域名，按 `wecom-setup.md` 验证。本次不等待外部工厂API。正式主机、数据库、域名及备份存储还需部署环境。第二模型供应商真实推理未验收；当前模型数据范围仍限定原有24个演示订单，新增项目仅做规则跟踪。无计费回执不填造费用。

部署和发布包说明见 `deployment-runbook.md`。版本包排除 `.env`、运行数据和依赖，包含源代码、测试、SQL、构建网页及逐文件SHA256清单；它不是已上线的服务实例。

---


## 本次交付验证（2026-09-07，取代下方历史状态）

- 全量单元与 PostgreSQL 集成测试 85 项通过，0 失败/跳过；TypeScript 检查通过。构建成功，主包约 1,032 kB（gzip 328 kB），仍有体积警告。
- `scripts/acceptance-smoke.mjs` 经真实本地 HTTP 接口完成新建 v1→配置 v2→附件 v3→百分比反馈 v4→批准交期/600 元成本变更并幂等执行 v5→报完工 v6→验收 v7，验证附件下载字节和调用结果查询。记录 `.runtime/acceptance-smoke.json`，订单 `E2E-1788769153227`。未请求模型。
- `scripts/patrol-smoke.mjs` 验证真实 Worker 巡检、提醒去重、异常负责人跟进、业务来源解除、解决及关闭；5 条审计、1 条提醒。订单 `PATROL-1788769484118`，记录 `.runtime/patrol-smoke.json`。结束恢复巡检策略为关闭；未请求模型。
- 备份目录 `.runtime/backups/2026-09-07T08-20-43.903Z`：两库真实 pg_dump 后恢复到临时库，校验业务 25 单、分析任务 8 条；临时库已删除，原库未被恢复覆盖。该快照早于巡检测试新增订单，当前订单数因此不同。
- 构建静态服务器真实监听验证通过：网页、API 反向代理及私有文件拒绝。页面可访问；浏览器快照验证当前订单、节点、功能入口及缺失预测依据提示。未声称已完成所有交互和多尺寸截图验收。

### 本次授权的真实模型重试

用户明确允许原有 24 单限定字段向 MiniMax 官方接口外发，重试 A26-019 失败分析。任务 `cbb0623f-a251-4fdc-811c-c080d4d7a100` 经 MiniMax-M3 一次尝试完成，实际查询版本 v5；旧失败快照保留在 `analysis_retry_audit`。重启后经 API 复核 completed/attempts=1/observed_version=5，证据接口返回 1 条对应 v5 快照，重试审计存在。

用量持久化代码在本次成功重试之后接入，历史任务没有回执，因此该任务用量未知；没有为补计量再次请求模型。后续模型回传计数会保存，缺失值保持 null，部分计量标记 partial，不推算价格。

自动审批拒绝沿用原配置重启，理由是可能触发本次重试外的模型外发。已采用安全替代：进程覆盖 `SOUNDLAB_MODEL_ANALYSIS=false` 启动，本地 .env 未改。经 `/api/runtime` 验证 Worker online=true、enabled=false；规则同步正常，自动模型分析关闭。巡检策略仍由页面单独配置。

### 范围与未验收项

本次实现项目配置、反馈证据、附件、依赖预测、规则巡检、异常生命周期、人工变更审批及执行、会话认证、签名 Channel 基础、能力声明和第二 HTTP 查询/反馈适配、运行管理、备份恢复。本地回归不代表正式上线。

真实 Channel 及外发提醒、工厂 API、完整第二系统、场景持久安装和字段映射管理、多租户 Worker/动态角色、模型审批恢复执行、模板库和批量导入、生产 CI/部署/告警仍需后续完成。外部渠道类型、工厂接口和正式部署环境尚待用户提供。参考实现不等于已适配任意系统。

---


## 退回返工闭环（2026-09-07）

新增固定业务能力 `sampling.node.return`。仅项目负责人可将待验收节点退回，原因必填，校验订单版本；已验收节点不可撤销。退回后进入rework，新轮次进度归零，保留上轮反馈、退回人/时间/原因/依据版本，反馈携带轮次。高风险标记不被返工操作降级。复用原有事务幂等和Outbox，模型仍无写能力；退回原因原文不进入模型工具结果。

浏览器真实操作A26-019：v2待验收→陈静填写原因并退回至v3；移动端赵强可见“返工待反馈”、0%及退回原因；模型一次完成v3分析并提示核实返工范围。移动端重新反馈20/20台成为v4待验收；陈静复检通过后成为v5已验收。数据验证round=1、返工记录1条、验收sourceVersion=4，历史完整保留。

最终v5模型分析尝试3次失败；已核实最后一次Harness `turn/end.reason.error.code=TIMEOUT`，且尚无查询工具回执。业务验收已完成，分析失败不回滚业务。补充了运行错误分类，页面显示模型请求超时；原任务保留failed/attempts=3，仅依据日志补记错误代码，没有追加重试或伪造成功。v2/v3/v4分析成功。

33项单元测试、5项真实PostgreSQL集成测试、类型检查和构建通过。覆盖返工权限/原因/版本、重复退回幂等、轮次与历史保留、再次验收、返工建议状态依据、错误信息脱敏。构建约995kB主包警告仍存在。下方“不支持退回返工”的历史状态由本节替代，已验收撤销和依赖交期评估仍不支持。

## 人工节点验收闭环（2026-09-07）

新增业务能力 `sampling.node.accept`，通过既有固定平台路由、通用网关、参考适配器访问独立业务服务。业务服务限制演示项目负责人陈静操作，仅可验收reported_complete且100%的节点，校验预期版本并记录验收人、时间、说明及依据版本。状态/审计/幂等回执/Outbox同事务提交；重复请求不重复写入。模型工具仍只有只读查询，未获得验收能力。

真实验证：A26-018装配反馈20/20台后成为v6待验收；桌面确认窗口由演示身份陈静填写“演示验收：20台数量核对及装配检查通过。”并提交后成为v7已验收。浏览器显示验收记录、当前节点转为声学测试；MiniMax分析一次完成并引用v7，建议不再包含装配验收。业务查询验证验收人chen、sourceVersion=6、记录持久保存。

28项单元测试、4项数据库集成测试、类型检查和构建通过。新增覆盖越权、版本冲突、非待验收状态、重复验收、说明校验、幂等事件以及199/200四舍五入为100%仍不允许报完工的数量边界。构建主包约994kB警告仍存在。

目前只支持验收通过，未实现退回返工/撤销验收；不因验收自动清除订单风险标记、不自动启动下一节点。风险重评估及交期变更审批另行建设。当前身份仍为本地演示白名单，并非生产认证。

## 事实与建议分离验收（2026-09-07）

新增严格结构化输出契约：模型只能返回订单号、实际版本、节点ID及核实项枚举；拒绝额外事实字段、未知节点、过期版本、无对应节点状态依据的建议。完整授权查询快照保存在工具回执中；平台按该快照生成进度、负责人和计划完成日期。模型不再生成这些事实的自然语言描述。模板核实项限于阻塞、启动准备和验收，不代表建议完整性已得到保证。

真实浏览器从移动反馈页选择周敏，给A26-018装配节点提交15/20台及演示待料说明，保存为v5。真实事件Worker经MiniMax-M3和Harness调用业务能力后一次完成；桌面显示“业务事实”与“助理建议”，整单54%、装配75%、声学测试计划完成2026-09-12，来源v5。未修改业务日期、负责人或验收状态。旧v4分析保留追溯并默认折叠，不作为当前结论呈现。

25项单元测试及类型检查通过；构建通过，仍有约992kB主包警告。本次不变更数据库结构/队列逻辑，3项数据库集成测试沿用上一轮。持续分析维持已授权开启及5秒队列检查。

下方“模型可能混淆完成日与启动日”的自由文本阶段限制，已由上述结构化结果替代。依赖交期评估、自动催办、真实Channel、审批写入仍未实现。

## 持续分析启用与频率配置（2026-09-07）

用户明确允许持续发送后续演示订单状态至 MiniMax 后，已将本地开关设为true，移除进程级false覆盖并重启。默认 `SOUNDLAB_ANALYSIS_INTERVAL_MS=5000`，可配置1000..3600000毫秒；每轮分析完成后等待该间隔再检查任务，无事件不调用模型，不是定时全量巡检。

真实Worker已完成A26-018 v4任务。首轮发现输出夹带过程草稿且进度口径混淆，补上最终答案清理、整单加权进度及节点计划字段后，重算结果引用v4、整单53%、装配70%。API与桌面浏览器均确认“模型分析已启用”和“分析完成”，尝试次数2（含修正重算），无自动业务写入。22项单元测试及类型检查通过；数据库3项测试沿用上一轮结果。

此验证覆盖触发、实际查询、证据版本、模型结果持久化及页面展示，不代表全文语义自动校验：当前生成文本仍可能把节点计划完成日理解成启动日或推断依赖，页面标为AI分析供核实，业务节点表仍为权威事实。后续应以结构化结论/事实引用进一步约束。以下“等待持续外发授权”的历史记录已由本次授权和启用替代。

## 事件分析 Worker 更新（2026-09-07）

已实现持久分析任务、独立模型执行循环、默认关闭的显式启用开关、3次退避重试、单实例锁及重启恢复、SDK限时执行与按模型隔离凭据、来源版本验证、页面状态及SSE更新。领域分析留在声研场景包，SDK进程生命周期留在运行适配包。数据库集成测试3/3通过，新增测试使用独立临时Schema验证任务去重、退避、恢复与重试上限。

本地服务已在 `SOUNDLAB_MODEL_ANALYSIS=false` 下重启，`/api/runtime` 返回 `enabled:false,online:true`。演示订单 A26-018 提交14/20台反馈后版本升至4；对应任务 `43d7fd58-cd3c-409d-b690-c1b90dd678da` 状态 `queued`、尝试次数0，业务记录及队列落库。浏览器检查桌面显示“规则同步 · 模型分析未启用”和分析面板；后续浏览器标签失效，反馈后页面刷新未完成再次核验，API已确认待处理状态。

真实自动模型分析未执行：自动审批两次拒绝开启持续外发，要求用户明确批准将后续演示订单数据自动发送至MiniMax。此前已通过的独立只读工具验证仍有效，但不能替代本轮Worker端到端验收。下一步取得持续分析授权后，开启开关、重启、处理已排队的v4任务，核验真实模型结果、证据和桌面显示。模型输出的语义正确性仍需人工核实；不自动修改业务数据。

以下为此前阶段记录；其中“Worker尚未实现模型推理”由上述实现状态替代，真实自动运行仍待验收。

验证日期：2026-09-07。实施目录：`.worktrees/soundlab-platform`，分支：`codex/soundlab-platform`。本记录只覆盖当前本地演示，不表示完整阶段计划或 A1—A9 已通过。

## 已实现的链路

React 桌面/移动端 → 平台场景固定 HTTP 路由 → 通用能力网关 → 打样适配器 → 独立 Fastify 业务服务 → 业务 PostgreSQL。

业务变更和 Outbox 同一事务提交；独立 Worker 按游标读取事件，将去重后的记录和消费游标同事务写入平台 PostgreSQL；SSE 通知前端刷新。平台核心网关不读取打样表，不导入打样领域包。场景和适配器承担领域映射。

当前只有一个真实业务提供方。替换能力提供方和租户绑定已由网关单元测试验证；尚不能据此宣称适配任意业务系统。

## 验证证据

| 验证 | 结果和范围 |
|---|---|
| `pnpm typecheck` | 通过，当前应用 TypeScript 静态检查 |
| `pnpm test` | 12/12 通过：模型路由、探针生命周期、网关替换/边界、节点数量/版本/负责人/验收保护 |
| `pnpm test:integration` | 2/2 通过：真实 PostgreSQL 租户隔离、幂等重放、冲突、越权、并发版本竞争、事务 Outbox；平台数据库角色不能读取业务表 |
| `pnpm build` | 通过；主 JS 约 990 kB，gzip 约 317 kB，存在大于 500 kB 的构建警告，尚未优化分包 |
| 桌面浏览器 | 24 单总览、筛选入口、节点详情可用；移动提交后通过 SSE 自动显示新进度和异常 |
| 移动浏览器 | 390×844 检查；提交装配 13/20、网罩待料说明成功，节点 65%、订单加权进度 52% |
| 负责人调整 | 陈静通过桌面将装配负责人调整为周敏，版本递增；刷新后负责人和历史反馈保留 |
| 浏览器日志 | 检查时未发现 error/warn；不是完整无障碍或多浏览器验收 |

验证产生的示例反馈和负责人调整保留在本地数据库中。演示切换身份会影响可反馈节点；移动端只列出该身份有负责节点的订单。

## 当前限制与后续顺序

更新（2026-09-07）：用户明确授权24个演示订单外发后，`node scripts/probe-model.mjs --business` 真实运行通过，退出码0，输出 `modelRequestSent:true,responseVerified:true,businessToolCalls:1,orderCount:24,capability:"sampling.orders.list"`。链路为 MiniMax-M3 → Harness 场景工具 → 平台固定接口 → 通用能力网关 → 参考适配器 → 业务服务。未执行业务写入。

本次运行目录：`.runtime/model-probe/47acda04-69c2-4639-a10f-128216fe91aa/`，`verification.json` 保存验证结果，`business-receipts.jsonl` 保存查询回执及对象版本。验证证明实际工具执行和订单总数一致，不代表逐条异常分析的语义正确性已验收。前一轮15项本地测试及类型检查通过，本次只更新验收文档。

1. 当前 Worker 是确定性事件消费者，没有运行模型推理。独立 Harness 探针已验证 MiniMax 鉴权、推理和只读业务工具调用，尚未接入持续运行的事件 Worker。
2. 真实模型下一步经 runtime adapter 接入通用能力网关，保持模型供应商路由独立。MiniMax M3 为默认配置；凭据只通过本地环境提供。
3. 演示身份由本地白名单选择，不是生产认证。服务仅监听回环地址；真实渠道必须增加可信身份映射和授权。
4. 未实现真实 Channel、S3 上传、验收操作、审批及审批后恢复、依赖交期预测、持久化定时跟进。页面不伪造这些执行结果。
5. 风险种子是演示标签，节点阻塞由反馈事实触发。当前事件摘要只表述对象版本变化，不构成智能分析。
6. 后续补齐契约运行时 Schema、独立第二适配器及完整故障验收，再逐项验收阶段计划。当前代码和文档未提交到 Git。
